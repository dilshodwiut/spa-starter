import type { CustomRoute } from "@/types";
import SomeIcon from "./components/some-icon";
import {{pascalCase name}} from "./views/{{kebabCase name}}";

const {{camelCase name}}Routes: CustomRoute = {
  id: "{{snakeCase name}}",
  title: "{{titleCase name}}",
  path: "{{kebabCase name}}",
  element: <{{pascalCase name}} />,
  Icon: SomeIcon,
  children: [],
};

export default {{camelCase name}}Routes;
